import 'package:flutter/material.dart';

class Conf extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Container(
        child: new Center(
            child: new Icon(Icons.settings, size: 300.0, color: Colors.grey)
        )
    );
  }
}